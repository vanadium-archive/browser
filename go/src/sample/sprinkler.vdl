package sample

type Sprinkler interface {
 Start(duration uint16) (error)
 Schedule(startTime string, duration uint16) (error)
 Stop() (error)
}

//TODO(aghassemi) cleanup comments and interface definition, add return types,