module.exports = methodStart;

/*
 * TODO(alexfandrianto): When adding the running status, this is where I'll
 * modify the state's output to add a new row saying that this method is in-
 * progress.
 */
function methodStart(state, method) {
  // pass for now.
}